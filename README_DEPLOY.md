# Rabbit Planner EN — Deploy

| Item | Value |
|------|-------|
| GitHub | `andyobrzut/plannercoelho` |
| Branch | `coelho-en` (also merged to `main` for Vercel) |
| Vercel project | `plannercoelho.en` |
| URL | `https://plannerrabbit.vercel.app` |
| Product slug | `planner-rabbit-en` |
| Code prefix | `RABBIT` |

## Vercel env vars

```env
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_PRODUCT_SLUG=planner-rabbit-en
```

After changing env vars, **Redeploy** (required for Vite).

## Painel central

Create codes with prefix **RABBIT** and slug **planner-rabbit-en** in `painelcodigos.vercel.app`.
